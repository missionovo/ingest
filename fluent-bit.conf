[SERVICE]
    flush        1
    daemon       Off
    log_level    info
    parsers_file parsers.conf
    plugins_file plugins.conf
    http_server  Off
    storage.metrics off

[INPUT]
    name tail
    tag auditd
    path /var/log/audit/audit.log

[INPUT]
    name tail
    tag nginx
    path /var/log/nginx/access.log

[FILTER]
    Name record_modifier
    Match *
    Record hostname ${HOSTNAME}

[OUTPUT]
    name  s3
    match *
    s3_key_format /${CUSTOMER}/$TAG/%Y/%m/%d/$UUID.txt
    upload_timeout 1M
    bucket troller-logs
    compression gzip